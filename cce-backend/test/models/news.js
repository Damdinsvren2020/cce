class News {
  constructor(id, mealId, title, imageUrl, info_title, info_text) {
    this.id = id;
    this.mealId = mealId;
    this.title = title;
    this.imageUrl = imageUrl;
    this.info_title = info_title; 
    this.info_text = info_text;
  }
}

module.exports = News;

// class News {
//     constructor(id, date, text, title, publisher, categoryid, categoryname, image, che, info_title) 
//     {
//       this.id = id;
//       this.date = date;
//       this.text = text;
//       this.title = title;
//       this.publisher = publisher; 
//       this.categoryid = categoryid;
//       this.categoryname = categoryname;
//       this.image = image;
//       this.che = che;
//       this.info_title = info_title;
//     }
//   }
  
//   export default News;