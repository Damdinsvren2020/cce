class Category {
  constructor(cid, title, imageUrl, root, catTest) {
    this.cid = cid;
    this.title = title;
    this.imageUrl = imageUrl;
    this.root = root;
    this.catTest = catTest;
  }
}

module.exports = Category;
